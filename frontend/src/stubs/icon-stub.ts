import { defineComponent } from 'vue'
// Stub for ~icons/* imports used by frappe-ui's TextEditor.
// This app does not use TextEditor, so icons are no-ops.
export default defineComponent({ template: '<span />' })
