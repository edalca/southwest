// Stub for ~icons/* imports used by frappe-ui's TextEditor component.
// Since this app does not use TextEditor, these are no-op components.
import { defineComponent } from 'vue'
const Icon = defineComponent({ template: '<span />' })
export default Icon
